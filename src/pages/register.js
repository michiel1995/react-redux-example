import React from 'react';
import Layout from '../components/Layout';
import LogoBlock from '../components/LogoBlock';

const Register = () => (
  <Layout>
    <Layout.Split>
      <LogoBlock />
      <Layout.Content>2</Layout.Content>
    </Layout.Split>
  </Layout>
);

export default Register;
